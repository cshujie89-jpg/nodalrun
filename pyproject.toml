[project]
name = "ai-runtime-os"
version = "0.1.0"
description = "Reliable runtime for AI workers"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

